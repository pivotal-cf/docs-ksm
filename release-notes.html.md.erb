---
title: Release Notes
owner: Platform Engineering (KSM Team)
---
<%# The below uses the local product variable and math so we do not need to update this page
with every release %>
<% previous_version = (product_info["local_product_version"].to_f - 0.1).round(2) %>
<% current_version = product_info['local_product_version'] %>


<strong><%= modified_date %></strong>

These are release notes for <%= vars.product_full %> (<%= vars.product_short %>).

<%= partial vars.path_to_partials + "/upgrade-planner" %>

<%= partial vars.path_to_partials + "/ksm/incompatible" %>

##<a id="0-9-93"></a> v0.9.93

**Release Date:** June 8, 2020

### Features

New features and changes in this release:

+ **Operators and developers can view <%= vars.product_short %> server logs using the KSM CLI:**
  You can use these logs to troubleshoot <%= vars.product_short %>.
  For more information, see [View <%= vars.product_short %> Logs](./troubleshoot.html#view-logs).
+ **Operators now register and set a default Kubernetes cluster using the KSM CLI:**
  Previously, operators could only do this by configuring the tile.
  For more information, see
  [Kubernetes Cluster Registration](./managing-clusters.html#registration-management).
+ **Operators can list existing service instances using the KSM CLI:**
   You can filter the list by marketplace name or cluser name.
   For more information, see [List Service Instances](./using.html#list-instance).

### Known Issue

This release has the following issue:

<%= partial vars.path_to_partials + "/ksm/custom-resources" %>
