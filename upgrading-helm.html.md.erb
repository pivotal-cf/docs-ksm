---
title: Upgrading KSM Using Helm
owner: Platform Engineering (KSM Team)
---

<%# The below uses the local product variable and math so we do not need to update this page
with every release %>
<% previous_version = (product_info["local_product_version"].to_f - 0.1).round(2) %>
<% current_version = product_info['local_product_version'] %>


<strong><%= modified_date %></strong>

This topic explains how to upgrade <%= vars.product_full %> (<%= vars.product_short %>) using Helm.

<%= partial vars.path_to_partials + "/upgrade-planner" %>

## <a id="overview"></a>Overview

<%= partial vars.path_to_partials + "/ksm/incompatible" %>

To upgrade from <%= vars.product_short %> v<%= previous_version %>:

1. Uninstall <%= vars.product_short %> v<%= previous_version %>.
   See [Uninstall <%= vars.product_short %>](#uninstall) below.

1. Install  <%= vars.product_short %> v<%= current_version %>.
   See [Installing and Configuring <%= vars.product_short %>](./installing-helm.html).

1. Add and enable access to your service offerings. See [Add a Service Offering to <%= vars.product_short %>](./using.html#add)
   and [Enable Service Offering Access](./using.html#enable).

## <a id="uninstall"></a> Uninstall <%= vars.product_short %>

To uninstall <%= vars.product_short %>:

1. View your installed releases by running:

    ```
    helm ls -a -A
    ```
    Record the name of the <%= vars.product_short %> release.
1. Uninstall <%= vars.product_short %> by running:

    ```
    helm uninstall RELEASE-NAME
    ```
    Where is `RELEASE-NAME` is the name of your  <%= vars.product_short %> release.
